bin/
recordings/
mediamtx.log
*.zip

# Real config with camera credentials — copy from mediamtx.yml.example, fill in creds, never commit
mediamtx.yml

# Predator control kit runtime artifacts
predator/run/
predator/logs/
